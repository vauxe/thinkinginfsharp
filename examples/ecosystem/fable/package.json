{
  "name": "@thinking-in-fsharp/fable-sample",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "restore:dotnet": "dotnet tool restore && dotnet restore FableSample.fsproj --locked-mode",
    "build:fable": "dotnet fable --outDir generated --noRestore --noCache",
    "dev": "pnpm run restore:dotnet && dotnet fable watch --outDir generated --noRestore --run pnpm exec vite",
    "build": "pnpm run restore:dotnet && pnpm run build:fable && vite build",
    "smoke": "node ../../../tests/site-fable-smoke.mjs"
  },
  "devDependencies": {
    "vite": "6.4.3"
  }
}
